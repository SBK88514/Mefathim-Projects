import random
import matplotlid


class RandomWalk:
   def __init__(self, dimensions,staps):
       self.dim = dimensions
       self.st = staps
       self.coordinate_system = \
       

    rw = (RandomWolk)()
